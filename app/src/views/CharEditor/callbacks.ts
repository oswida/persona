export const buttonPress = (jseditor: any, e: any) => {
  // jseditor.options.button.roll/roll_param
  //   let roll = jseditor.options.button.roll as string;
  //   if (!roll) return;
  //   const param = jseditor.options.button.roll_param;
  //   if (param) {
  //     const editor = editorRef.current.getEditor(param);
  //     if (!editor) return;
  //     const value = editor.getValue();
  //     roll = roll.replace("{param}", value);
  //   }
  //   const [result, msg] = rollDice([roll]);
};
